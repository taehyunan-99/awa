# 런타임 산출물 — 정의가 아닌 결과물 (PROJECT_ROOT 별 격리)
.agent-harness/

# Claude Code 개인·런타임 설정만 ignore, skills 는 공유 (R5: .claude/ 통째면 !예외 안 먹힘)
.claude/*
!.claude/skills

# 다른 에이전트(Codex/Antigravity 등) 로컬 미러 — 정의는 .claude/skills 가 단일출처
.agents/

# 설계/계획 문서 — git 추적 안 함
docs/

# superpowers brainstorming/visual-companion 로컬 세션 산출물
.superpowers/

# arena 실험 마지막 실행 메타 — 매 가동마다 덮어쓰는 임시
tests/lead-arena/.last-run

# 캐시 / OS / 에디터
*.log
.DS_Store
.cache/
__pycache__/
*.py[cod]
.venv/
node_modules/
.idea/
.vscode/
*.swp
*~
.claude/
config/.lead-auto-allow-marker
config/*.bak
