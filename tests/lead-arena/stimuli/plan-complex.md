# 확정 plan (이번 가동의 작업 계획)
## 작업
간단한 문자열 유틸 라이브러리를 만든다.
1. 코어: src/strutil.py 에 slugify(s) 함수 (소문자화·공백→하이픈).
2. 테스트: tests/test_strutil.py 에 slugify 단위테스트 3케이스. 코어가 먼저 있어야 함(입력=src/strutil.py).
3. 문서: README 에 slugify 사용 예시. 테스트 통과 후 작성(입력=테스트 결과).
